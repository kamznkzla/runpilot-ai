{
  "name": "runpilot-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Static RunPilot AI running training planner.",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve .",
    "build": "echo Static site: no build step required."
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
